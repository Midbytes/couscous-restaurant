query getDelicacies($locale: I18NLocaleCode) {
  delicacies(locale: $locale) {
    data {
      attributes {
        name
        description
        price
        index
        type
      }
    }
  }
}

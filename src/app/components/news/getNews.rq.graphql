query getNews($locale: I18NLocaleCode) {
  newsPosts(locale: $locale, sort: ["Title"]) {
    data {
      id
      attributes {
        Title
        Description
        backgroundBanner {
          data {
            attributes {
              url
            }
          }
        }
      }
    }
  }
}

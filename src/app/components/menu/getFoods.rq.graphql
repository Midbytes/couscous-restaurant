query getFoods($locale: I18NLocaleCode) {
  foods(locale: $locale) {
    data {
      id
      attributes {
        name
        description
        price
        course
        index
        type
      }
    }
  }
}
